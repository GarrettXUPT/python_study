今日主要内容：
    1、什么是列表
        定义：能装对象的对象
        在python中使用[]来表示列表，内部元素使用逗号隔开，而且对数据类型没有要求
        列表存在索引和切片，并且和字符串是一样的

    2、相关的增删改查操作（重点）
        添加：append（）追加,在列表后面进行添加
             insert（位置，元素），表插入指定元素到指定位置
             extend 表示迭代添加，仅针对可迭代对象，比如字符串等
        删除：pop（）内容可以加索引 也可以不加，不加索引表示默认从后往前删除元素，加上就是删除指定位置的元素
             remove（元素） 根据元素进行删除，删除指定元素
        修改：lst[索引] = 元素
        查询：for 循环查找

    3、列表的嵌套
        多个列表互相嵌套

    4、列表的其他操作
        count（）计数
        sort（）排序 reverse= True 默认为升序排序
        reverse（） 将列表翻转

    5、什么是元组
        能装对象的对象，不可变，一旦确定，就不更改
        只读列表 可以迭代

    6、元组的嵌套
        和列表相同，可以互相进行嵌套
        但是元组不可变，指的是指向不变，但内部元素中的元素可进行调整

    7、range（重点及难点）
        数数
        for i in range(len(lst)):
            print(i, lst[i])  得到列表的索引及对应的元素
