今日主要内容：
    1、os
        主要是针对操作系统的，一般用来操作文件系统

    2、sys
        主要是针对python解释器   
        sys.path(重点中的重点)

    3、namedtuple
        命名元祖——>类似于创建了一个类

    4、序列化
        将一个对象拍散，叫序列化
        将拍散的对象还原回原来的对象，即为反序列化

    5、pickle（重点）
        作用：将一个对象进行序列化操作
        1、dumps() 将对象序列化成字节
        2、loads() 将序列整合为对象
        3、dump() 将序列化后的内容存储到文件中
        4、load() 将储存在文件中的内容还原为对象

    6、json(数据格式)（重点）
        json可以认为是python中的字典，有一点点不一样
        python: True, False, None
        json: true, false, null

        1、dumps() 将对象序列化成json
        2、loads() 将json整合为对象
        3、dump() 将json后的内容存储到文件中
        4、load() 将储存在文件中的json内容还原为对象