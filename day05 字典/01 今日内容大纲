今日主要内容：

    1、什么是字典
        字典以key：value 的形式来保存数据
        用{}表示，存储的是key：value

    2、字典的增删改查（重点）
        1、字典的添加
            dic[新key] = 值  表示对字典进行添加
        2、字典的删除
            pop（key）
        3、字典的修改
            dic[老key] = 值
            updata（）更新字典
        4、字典的查询
            dic[key] = value
            get(key, 值） 该种方法可能会进行报错
            get(key, values, "提示语") 该种查找方法可以使程序不进行报错，而是用提示语提示用户
            setdefault（key，value) 先查找是否存在该key若存在则执行查找操作，返回value值，若不存在，则执行添加操作
        5、字典的遍历（字典为可迭代对象）
            for k, v in dict.items():
                print(k,v)

    3、字典的嵌套
        字典套字典

    4、字典的相关操作

    5、集合（不重要） 主要用于去重复，集合中的元素是不重复的，而且必须是可哈希的（不可变），字典中的key
        空集合：set（）
        空列表：list[]
        空元组：tuple（）
        非空集合：{123}，字典：{“呵呵”：“你好”}
        集合其实就是不存value的字典


        可变的：列表
       不可变的：字符串 int bool 元组（tuple）