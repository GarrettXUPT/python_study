操作系统：一个大型程序，受硬件保护，操作系统就是一个协调、管理和控制计算机硬件资源和软件资源的控制程序
    电影：模仿游戏，华尔街之狼，大空头
    IO操作：input output,提高代码运行效率就要规避这些IO操作
        多道技术：空间复用，时间复用，而且进程之间是空间隔离的

并发：是伪并行，即看起来是同时运行。单个cpu+多道技术就可以实现并发，（并行也属于并发），并发是基于程序间的迅速切换，
     看起来是同时运行的，遇到IO切换的可提高代码效率，任务切换和保存状态（保持现场）

并行：同时运行，只有具备多个cpu才能实现并行

进程的三状态：就绪(等待操作系统调度到CPU中执行)  执行  阻塞

