今日主要内容：
    1、基础数据类型的补充
        大多数数据类型的知识已经学完了，现在只是补充
            join()   "*".join("马化腾")  将传递进去的参数进行迭代，获取到的每个元素和前面的*进行拼接，得到是字符串
            print（） 马*化*腾
            split（）切割的结果是列表

            列表和字典：都不能在循环的时候直接删除，如果直接删除，则会报错
            需要把要删除的内容记录在新列表中，在删除过程中，循环新列表，删除旧内容
            fromkey：返回新字典，与原来老字典没有关系；当value为可变的数据类型，各个key共享该可变的数据类型，其中一个被改变，那么全部都会被改变

    2、深浅拷贝（重点，难点）
        = 没有创建新的对象，只是将内存地址进行了复制，二者指向的是同一块内存，浅拷贝
        深拷贝：完全复制创建了一个新的对象，二者指向不同的内存地址

        浅拷贝 lst.copy() 只拷贝第一层 lst[:] 也属于浅拷贝
        深拷贝
            import copy
            copy.deepcopy() 会把对象内部的所有内容进行复制和拷贝